import { Placeholder } from "@/components/Placeholder";

export default function ZonesetupPage() {
  return <Placeholder title="Zone setup" note="Create disaster zones, set eligible postcodes, tier payout amounts, and the hard budget cap. Coming in the admin phase of this build." />;
}
